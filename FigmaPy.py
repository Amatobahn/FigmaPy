# Only include this file in your project to handle backwards compatibility
# to support any old projects that still use `import FigmaPy` (camelCase)
# instead of `import figmapy` (lowercase)

from figmapy import *
